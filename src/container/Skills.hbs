<div>
    {{#with main}}
    <main>
        <section>
            {{> title data = "All Skills" }}

            <ul>
                {{#each skills}}
                <li>
                    {{name}}
                    {{#if point includeZero=true}} ({{point}}) {{/if}}
                    {{#if isTop}} {{Top}} {{/if}}

                    {{#unless point includeZero=true}} ( not learn){{/unless}}

                    {{#unless point}}
                    {{#if point includeZero=true}}(learn){{/if}}
                    {{/unless}}
                    {{!-- {{name}} {{point}}
                    {{#if isTop}} (top) {{/if}}
                    {{#if point includeZero=true}} (ще учу) {{/if}}
                    {{#unless point includeZero=true}} (not учу) {{/unless}} --}}
                </li>
                {{/each}}
            </ul>
        </section>
    </main>
    {{/with}}
</div>